# 星火智能游戏 · 预习学习指南

> 本指南对应《星火-智能游戏-预习材料.docx》，并配套了一套循序渐进的 Rhino 8 Python 学习 Demo。
> 你现在的目标：**先学会 Cursor 的使用 + Rhino 8 建模与 CLI 控制**（文档第一、二、三部分）。

---

## 一、活动完整流程（先理解全局）

```
AI Agent 生成建模思路/脚本
        ↓
Rhino 8 完成几何建模
        ↓
检查 / 整理模型（单位、图层、命名、结构）
        ↓
插件 或 Datasmith 导入 UE5.7
        ↓
UE 中制作并测试简单游戏场景
```

本套 Demo 覆盖前 3 步（Agent + Rhino 建模 + 检查整理）。UE 部分见文档第四、五部分。

---

## 二、本套 Demo 与文档的对应关系


| Demo 文件                                 | 对应文档章节      | 你将学会             |
| --------------------------------------- | ----------- | ---------------- |
| `lesson_01_rhino_basics.py`             | 第二部分：基本对象   | 点、线、曲线、曲面、实体     |
| `lesson_02_bezier_curve.py`             | 第三部分：贝塞尔曲线  | 控制点如何决定曲线形状      |
| `lesson_03_bezier_surface.py`           | 第三部分：贝塞尔曲面  | 控制网格如何决定曲面       |
| `lesson_04_nurbs.py`                    | 第三部分：NURBS  | 权重、节点、有理曲线/曲面    |
| `lesson_05_brep.py`                     | 第三部分：Brep   | 实体的面/边/顶点结构      |
| `lesson_06_curve_to_form.py`            | 第二+三部分      | 由曲线生成曲面/体块       |
| `lesson_07_cli_control.py`              | 第二部分：CLI 控制 | 命令行与外部脚本控制 Rhino |
| `lesson_08_capstone_future_building.py` | 第五部分：示例场景   | 综合：由曲线生成未来建筑     |


---

## 三、Cursor 使用入门（文档第一部分）

Cursor 是基于 VS Code 的 **AI 编程编辑器**。预习阶段，你主要用它来：
**让 AI 读懂这些 Demo、解释概念、帮你改参数、生成新脚本。**

### 必会 4 个操作


| 操作       | 快捷键      | 用途                    |
| -------- | -------- | --------------------- |
| AI 对话    | `Ctrl+L` | 提问、解释代码、生成脚本          |
| 内联编辑     | `Ctrl+K` | 选中代码后让 AI 局部修改        |
| Agent 模式 | 聊天面板切换   | 让 AI 自动改多个文件          |
| 终端       | `Ctrl+`` | 运行外部 Python（如 CLI 控制） |


### 三个练习

1. **理解代码**：打开 `lesson_02_bezier_curve.py`，全选 → `Ctrl+L` → 问：
  > 请逐行解释这段贝塞尔曲线代码，控制点是如何影响曲线的？
2. **改参数**：在 `lesson_03_bezier_surface.py` 选中控制点列表 → `Ctrl+K` → 输入：
  > 把控制网格改成 4x4，让中间两个点更高
3. **生成新代码**：`Ctrl+L` → 输入：
  > 参考 lesson_02，帮我写一条"二次贝塞尔曲线"（3 个控制点）

### 判断 Agent 结果是否正确（文档反复强调）

- AI 生成的几何**不一定对**，一定要在 Rhino 里运行、肉眼检查；
- 检查：对象数量对不对？比例/单位合理吗？图层和命名清晰吗？
- 报错就把错误信息贴回 Cursor，让它修。

---

## 四、Rhino 8 三种控制方式（文档第二部分）

```
方式1  命令行 CLI        →  cli_batch_commands.txt（最简单）
方式2  内部 Python       →  ScriptEditor 里跑 lesson_01~06（核心）
方式3  外部 Python(COM)  →  lesson_07_cli_control.py（自动化）
```

### 如何运行内部 Python Demo

1. 打开 Rhino 8
2. 命令行输入 `ScriptEditor` 回车
3. 新建脚本，粘贴 `lesson_xx.py` 内容
4. 按 `F5` 运行
5. 视口看不到？命令行输入 `Zoom Extents`

---

## 五、数学建模四个核心概念（文档第三部分）

> 不要求会推导，只要"理解大致表示什么、解决什么问题"。


| 概念            | 一句话理解              | 解决的问题      |
| ------------- | ------------------ | ---------- |
| **Bézier 曲线** | 用少量"控制点"拉出的光滑曲线    | 自由造型的基础    |
| **Bézier 曲面** | 控制点变成网格，拉出的光滑曲面    | 自由曲面造型     |
| **NURBS**     | 给控制点加"权重"+"节点"的升级版 | 能精确表示圆/椭圆等 |
| **Brep**      | 用"面+边+顶点"描述一个实体的边界 | 表示封闭实体     |


它们的关系：

```
Bézier 曲线/曲面  →（加权重、节点，更通用）→  NURBS  →（多个面缝合）→  Brep 实体
```

---

## 六、建议学习顺序（5 天）


| 天     | 内容                                   |
| ----- | ------------------------------------ |
| Day 1 | Cursor 入门 + lesson_01 基础对象           |
| Day 2 | lesson_02 贝塞尔曲线 + lesson_03 贝塞尔曲面    |
| Day 3 | lesson_04 NURBS + lesson_05 Brep     |
| Day 4 | lesson_06 由曲线生成体块 + lesson_07 CLI 控制 |
| Day 5 | lesson_08 综合项目 + 准备课前提交内容            |


---

## 七、课前需要准备（文档第五部分）

1. 一个简单 Rhino 建模文件 / 脚本 → 跑完 lesson_08 另存为 `.3dm`
2. 一段用 Agent 生成/修改代码的记录 → 截图你在 Cursor 的对话
3. 对一个几何概念的简短理解 → 参考本指南第五节
4. 一个想在课堂实现的小场景想法 → 例如"可进入的几何迷宫"

